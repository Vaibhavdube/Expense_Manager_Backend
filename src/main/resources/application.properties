spring.application.name=moneymanager

#msql configuration
# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager
spring.datasource.username=root
spring.datasource.password=mnbvcxz

server.servlet.context-path=/api/v2.0

# MySQL Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL Dialect
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Port (optional)
server.port=8080

# Logging Configuration
# Brevo ka SMTP server use karo
#spring.mail.host=smtp.gmail.com
# communication gate
#spring.mail.port=587
#    Environment variable access ${} Username environment variable se lo
#email address vaibhav07154(from)
#spring.mail.username=${MAIL_USERNAME}
#spring.mail.password=${MAIL_PASSWORD}
# SMTP authentication ON pass, user verify
#spring.mail.properties.mail.smtp.auth=true
#TLS encryption ON   password plain travel nhi kerega encrypted
#spring.mail.properties.mail.smtp.starttls.enable=true
# Simple Mail Transfer Protocol
#spring.mail.protocol=smtp
#spring.mail.properties.mail.smtp.from=${MAIL_FROM_EMAIL}

brevo.api.key=${BREVO_API_KEY}
brevo.from.email=${BREVO_FROM_EMAIL}

jwt.secret=mysecretkeymysecretkeymysecretkey123456
  # 1 day in milliseconds
jwt.expiration=86400000

money.manager.frontend.url=http://localhost:5173

app.activation.url=${EXPENSE_MANAGER_BACKEND_URL}


spring.profiles.active=prod